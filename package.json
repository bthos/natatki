{
  "name": "natatki",
  "version": "0.1.0",
  "description": "Note-taking app with AI categorization and GitHub integration",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend",
    "build:shared": "npm run build -w @natatki/shared",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run build:shared && tsc --noEmit -p packages/backend && tsc --noEmit -p packages/web && tsc --noEmit -p packages/mobile",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
